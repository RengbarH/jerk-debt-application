import { mount } from "@vue/test-utils";
import JerkSingle from "@/components/JerkSingle";

/* Test 3 nicht fertig
describe("JerkSingle.vue", () => {
    it("should show page title", () => {
        const wrapper = mount(JerkSingle, {
            propsData: {
                jerks: [
                    {
                        id: 1,
                        firstname: 'Max',
                        lastname: 'AVC',
                        gender: 'MALE'
                    },
                    {
                        id: 2,
                        firstname: 'Maxi',
                        lastname: 'AVC',
                        gender: 'FEMALE'
                    }
                ]
            }
        })

        const jerksCards = wrapper.findAllComponents(JerkSingle)
        expect(jerksCards.length).toBe(2)
    })
})*/
